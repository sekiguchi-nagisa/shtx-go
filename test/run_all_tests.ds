#!/usr/bin/env ydsh

cd "$SCRIPT_DIR"
pwd

assert test -f /ydsh/tools/litecheck/litecheck
assert test -x ./run_test.ds

# run test cases

echo '@@run test: SHTX_DEV_MODE=on ./run_test.ds -b /ydsh/tools/litecheck/litecheck ./common'
SHTX_DEV_MODE=on ./run_test.ds -b /ydsh/tools/litecheck/litecheck ./common

echo '@@run test: SHTX_DEV_MODE=on ./run_test.ds -b /ydsh/tools/litecheck/litecheck ./base'
SHTX_DEV_MODE=on ./run_test.ds -b /ydsh/tools/litecheck/litecheck ./base